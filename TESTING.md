# v0.1.3 测试记录与反馈清单

本页记录实际验证范围，不把最低系统版本或静态对齐等同于所有设备运行通过。

## 本版真机验收

2026-09-26，BKQ AN80 / Android 16 / arm64 / 4 KB 页面设备，正式签名 **0.1.3 build 12**：

- 用户确认结果页“编辑 / 压缩 / 拼接”、文字添加与拖动、指定帧显示的操作与生成结果“都正常，符合预期”。
- 三个入口均自动带入当前 GIF。正式 App 大字号文字、描边、底色实际生成成功并保存到相册。
- 原生编辑回归 **44/44**，文字配方回归 **2/2**，Flutter 到原生完整流程 **1/1**。实际制作 GIF、添加中英文再分解，逐帧像素检查确认文字仅在指定两帧出现，原 GIF 保留。
- 修复 Android 正则对较长文字图层配方的误拒绝；使用真实失败的纯文字夹具和 Android 回归验证修复。
- 语言切换立即生效，重开保留；英文历史与日期已检查，最后恢复跟随系统。此前独立语言与相册用例 **4/4**；系统语言动态变化与手动优先另有组件测试。
- 最终覆盖安装前后历史均为 **24 条**，之后验证新增两条任务；没有逐 ID 比对全部历史。应用 ID、首次安装时间和数据目录身份未变，没有卸载或清理正式 App 数据。

发布文件与手机已验收 APK 完全相同，临时测试安装已移除；未使用模拟器。

## 电脑与发布文件检查

- Flutter **210/210**，静态分析无问题。
- Android JVM **397 通过、4 项环境条件跳过、0 失败**；lint **0 错误、17 既有警告**。
- APK 为 `com.local.gifconverter / 0.1.3 / 12`，最低 API 29、target 36，仅 arm64，非 debuggable；无联网或广泛媒体读取权限。
- 沿用公开版本正式证书，APK SHA-256：`8ef50ff9dfb7e9c499a24e8002c6d9b7be97f9b7a2676f0556283d5caafd1578`。
- 8 个原生库 API 29 依赖、构建身份及 ELF/ZIP 16 KB 对齐通过；五个 FFmpeg 库与 10,111 个源码文件和构建材料核对一致。
- APK、对应源码、许可、安装/发布/免责说明、公开证书和校验清单在同一 Release 提供，共 9 个附件。

## 其他系统与未覆盖项目

Android 10 的兼容反馈来自此前版本。本次文字批注、快捷处理和语言功能未在 Android 10～15 逐项复测；其他机型、真正 16 KB 页面环境仍待运行验证。没有完整重测所有既有功能，也未测各机型温升、耗电及长期大素材负载。语言检查与此次快捷处理/文字批注的用户验收分开记录。

旧公开版之间的更新和拼接旋转验收记录可查看此前 Release 与仓库提交。

## 反馈建议

请提供版本、手机型号、Android 版本、功能、参数、预期与实际结果。文字问题可说明内容、字号、样式及帧范围；语言问题注明系统语言和 App 选择；更新问题附安装来源与完整提示。

先用少量素材、短片段及较低输出尺寸尝试，重要结果另存。分享素材前去除个人信息并确认有权提供。安装和更新步骤见[安装说明](INSTALLATION.md)。
