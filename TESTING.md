# v0.1.0 测试验收

本清单针对正式签名 APK（安装版本 0.1.0，版本代码 6）。以下项目尚待使用这一份最终安装包确认；同内容本地预览版的通过记录与正式签名包的安装验收分别记录。

新图标、历史功能/日期筛选、分解预览与返回保留，以及日期拖动与固定按钮布局均已在同内容、原调试签名的本地预览包上获得用户确认。build 6 进一步将未选按钮文字居中，手机显示已核对，原有 118 条历史保留。正式包与预览包的 53 个非签名 ZIP 条目逐字节一致；预览包不属于下载附件，也不替代下方正式签名 APK 的安装验收。

## 安装前

- 使用 Android 12+、arm64-v8a 真机；核对文件名和 `SHA256SUMS.txt`。
- 如果装过旧内部测试版，先按[安装说明](INSTALLATION.md)另存重要结果并检查副本。签名不同不能直接覆盖，卸载会删除 App 内历史；不使用清空原数据的方法进行无确认的自动测试。
- 使用可公开或自己制作的少量测试素材；删除操作仅针对本次测试创建的记录。

## 基础流程

- [ ] 安装、冷启动与再次打开正常；设置中的“开源许可”可以查看，显示版本 0.1.0。
- [ ] 桌面和应用列表显示粉紫色举牌图标，人物与 GIF 字样清楚。
- [ ] 图片转 GIF：横竖图片排序、设置画布、生成、预览、保存相册正常，画面完整。
- [ ] 视频转 GIF：短视频的起止时间、裁剪与速度生效，结果和时长符合选择。
- [ ] GIF 编辑：片段、删帧、裁剪与旋转等操作生效，撤销/恢复和结果预览正常。
- [ ] GIF 压缩：前后体积正确；未进一步缩小时保留原 GIF。
- [ ] GIF / 视频分解：勾选、帧图预览、保存 PNG 与导出 ZIP 正常，数量与选择一致。
- [ ] GIF 拼接：至少两个 GIF，顺序播放与自定义画布的位置、比例、背景正确。
- [ ] 分享面板可打开，返回后预览正常；不必实际向他人发送。
- [ ] 新结果出现在生成历史，重启后可打开、另存；单删与批量删除只影响所选测试记录，已保存的相册副本保留。
- [ ] 历史功能多选、指定某天及起止日期得到正确结果；范围包含首尾两天，分解记录可预览及再次保存图片/ZIP，返回和刷新保留条件。
- [ ] 日历首尾圆点可跨行拖动，靠近上下边缘滚动月份、松手停止；点选及输入日期可用，取消不覆盖原条件。选项位置固定，未选文字居中。

测试时记录手机型号、Android 版本、出错功能和参数、预期与实际结果。没有测到的项目保持未勾选；其他设备与真正的 16 KB 页面环境需单独记录，不能用包体对齐检查替代运行验证。

## 已完成的电脑检查

基础历史筛选相关 Flutter 55/55，日期拖动与历史追加检查最终 24/24，文字居中后布局补查 2/2；静态分析无问题。Android 查询/存储/任务检查 30 通过、1 项 Windows 文件系统用例跳过。安装包的正式签名、版本、arm64 架构、权限和 ZIP 对齐已核对；10,111 个 FFmpeg 源码文件与五个库的对应关系已检查。第三方许可通知从本版 APK 重新提取，包含新增的中文日历依赖；配套文件 SHA-256 一致。

v0.1.0 已于 2026-09-24 作为公开测试版发布。上方未勾选的正式签名包真机项目仍待验证，公开发布不代表这些项目已通过；欢迎按清单反馈测试结果。
