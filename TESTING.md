# v0.1.2 测试记录与反馈清单

本页记录实际验证范围，不把最低系统版本或静态对齐等同于所有设备运行通过。

## 本版真机验收

2026-09-25，在 BKQ AN80 / Android 16 / arm64 / 4 KB 页面设备上，使用与本次发布完全相同的正式签名 **0.1.2 build 9**：

- 从公开 v0.1.1 覆盖更新，安装及冷启动正常，原有 8 条历史保留，旧记录能预览；应用 ID、首次安装时间和数据目录身份未变。
- 原生回归 9/9 通过：旋转后的尺寸/时长、越界及完全出界、顺序/左右/上下拼接、透明 disposal/零延时、取消与时长限制、四/六图三种画布比例、透明逐帧清空、最长边限制。
- 真机初轮发现小 GIF 放大时边缘淡化，增加 C 用例复现后修复边缘采样，最终全部用例通过。
- 正式 App 将竖图自动转 90°，再用按钮/滑块调整、放大、向左上越界拖动，返回布局后设置保留。
- 实际生成并保存透明测试 GIF：720×720，3 帧，200/300/400ms，无限循环；取回本次自建素材的输出，检查裁切、透明、叠放与帧间变化通过。
- 用户试用双指同时旋转/缩放、自动转向及出界生成后确认“都正常，符合预期”。

本次没有卸载或清理正式 App 数据；独立临时测试组件完成后已移除。没有使用模拟器。

## 电脑与发布文件检查

- Flutter 全量 191/191，最终定向 21/21，静态分析无问题。
- Android JVM 391 通过、4 项环境条件跳过、0 失败；lint 0 错误、17 警告。
- C 像素检查覆盖直角转向、360 个角度边界、透明叠加、越界、取消与小图放大边缘。
- APK 为 `com.local.gifconverter / 0.1.2 / 9`，最低 API 29、target 36，仅 arm64，非 debuggable；无联网或广泛媒体读取权限。
- 沿用公开版本正式证书，发布 APK SHA-256 与真机验收文件完全一致：`1326732dfa5fb327a427dd106382b048b23717015e99fc0289779b66f63f6125`。
- 8 个原生库 API 29 依赖、构建身份及 ELF/ZIP 16 KB 对齐通过；五个 FFmpeg 库与 10,111 个源码文件和构建材料核对一致。
- APK、对应源码、许可、安装/发布/免责说明、公开证书和校验清单在同一 Release 提供，共 9 个附件。

## 其他系统与未覆盖项目

上一版 Android 10 独立兼容测试包收到 Android 10（机型未提供）与 Android 16 的正常使用反馈。本次新增旋转功能未在 Android 10～15 逐项复测，其他机型与真正 16 KB 页面设备仍待补充运行验证。此次没有完整重测所有既有功能，也未测各机型温升、耗电及长期大素材负载。

此前公开 v0.1.0 → v0.1.1 在同一 Android 16 设备上的覆盖更新、示例历史保留与 APK 一致性检查已通过，详细历史可查看旧 Release 和仓库提交。

## 反馈建议

请注明实际 App 版本、手机型号、Android 版本、功能、参数、预期和实际结果。旋转问题可附角度、画布比例、素材数量与出界位置；更新问题附安装来源和完整系统提示。

先用少量素材、短片段及较低输出尺寸尝试。删除测试只使用不需要的记录；重要结果请另存。分享素材前去除个人信息并确认有权提供，无需发送完整私人相册。安装和更新步骤见[安装说明](INSTALLATION.md)。
