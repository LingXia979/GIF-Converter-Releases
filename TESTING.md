# v0.1.1 测试记录与反馈清单

## 已收到的真机反馈

2026-09-24，维护者确认自己和另一位测试者使用 Android 10 独立兼容测试版后，没有发现问题，并说明系统分别为 Android 16 和 Android 10。

| 设备 / 系统 | 反馈 | 记录范围 |
| --- | --- | --- |
| Android 10，型号未提供 | 使用正常 | 独立兼容测试版 Test 1；未提供逐项检查记录 |
| BKQ AN80 / Android 16 | 使用正常 | 维护者反馈；独立兼容测试版 Test 1 |

本次发布的 `0.1.1`（build 7）使用原公开版的包名与正式签名，兼容改动与上述测试版相同。正式 APK 另做下方包体检查和公开包覆盖更新验证；没有将测试版反馈扩写为最终包的逐项验收。Android 11～15、其他机型、真正的 16 KB 页面设备和各机型温升/耗电仍待补充。此次未使用模拟器。

## 公开包覆盖更新验证

2026-09-24，在 BKQ AN80 / Android 16 / arm64 / 4 KB 页面环境中，完成以下检查：

- 安装本仓库公开的 v0.1.0，用项目图标生成 1 条 159.7 KB 的 GIF 示例历史。
- 直接覆盖安装公开 v0.1.1，安装成功，冷启动 200ms；版本代码从 6 升至 7，应用 ID、首次安装时间及数据目录 inode 保持不变。
- 更新后示例历史仍为 1 条，原记录可打开预览；没有重新生成该结果。
- 从手机拉取实际安装 APK，SHA-256 为 `2d47da731d7c063fda3033e704c918c684b38f7f6c68a298e290a636530bb154`，与 GitHub 发布资产一致；证书为公开正式签名。

此检查验证同一正式签名通道的覆盖升级与已有结果预览，不代表跨签名历史迁移或全部功能验收。后续从本仓库下载安装同通道新版即可更新，无需先卸载。

## 已完成的电脑与发布包检查

- Android JVM：387 通过、4 项符号链接/文件删除平台条件用例跳过、0 失败；lint 0 错误、17 警告，无 NewApi 错误。
- Flutter 原生构建身份与诊断界面：7/7 通过。既有历史筛选、日期拖动、文字居中检查及用户验收记录保留。
- 正式 APK：版本 `0.1.1 / 7`，包名 `com.local.gifconverter`，最低 API 29，target 36，仅 arm64，非 debuggable；无联网或广泛媒体读取权限。
- 签名通过验证，与公开 v0.1.0 的证书一致；旧发布 APK 未修改。正式版未使用独立测试密钥。
- 包内 8 个原生库的强符号引用、API 29 依赖、构建身份及 ELF/ZIP 16 KB 对齐检查通过。静态对齐不代表 16 KB 真机测试通过。
- 五个 FFmpeg 动态库对应当前 API 29 构建；10,111 个源码文件、完整许可及配套文件 SHA-256 已核对。

## 可继续补充的真机检查

请先按[安装说明](INSTALLATION.md)安装：已装公开 v0.1.0 可直接更新；“兼容测试”版历史独立；更早同包名调试版签名不同。测试记录注明实际版本、型号和 Android 版本。除上述已验证的安装、升级及示例历史预览外，以下项目仍需对最终正式包逐项补充。

- [x] BKQ AN80 / Android 16：公开 v0.1.0 → v0.1.1 覆盖升级与冷启动正常，示例历史保留且能预览。
- [ ] 设置保留、多次重开及其他机型安装与更新。
- [ ] 图片转 GIF：选图、排序、画布/补边、生成、预览与相册保存正常。
- [ ] 视频转 GIF：片段、裁剪、速度和帧率生效；前后台切换、通知和取消正常。
- [ ] GIF 编辑、压缩、GIF/视频分解及顺序/画布拼接结果正常。
- [ ] GIF 分享面板可打开；分解可保存 PNG 和 ZIP。
- [ ] 历史重开保留，功能/日期筛选、日期首尾拖动、预览返回正常。
- [ ] 单删或批量删除只影响所选测试记录；原素材和另存副本保留。
- [ ] 图标与设置中的版本、开源许可显示正常。

建议先用短片段、少量图片和较低输出尺寸。删除操作只针对不需要的测试记录。反馈请提供功能、参数、预期与实际结果；素材请去除个人信息并确认可分享，无需提供完整私人相册。
