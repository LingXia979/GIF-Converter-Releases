# GIF Converter v0.1.3 测试版：安装与使用

这是供体验和反馈的 Android 测试版，支持制作、编辑、压缩、分解和拼接 GIF，以及本地生成历史。安装版本为 **0.1.3**，版本代码为 **12**；本次增加结果快捷处理、文字批注与中英文切换，继续支持 Android 10+。

**更新时提示“签名不一致”，请先保留现有 App，不要卸载或清除数据。** 同名 App 可能来自早期内测或公开发布，两者的更新包不同；仅凭桌面名称和版本号不能区分签名。

| 已安装来源 | 应使用的更新方式 |
| --- | --- |
| 从 GitHub Release 下载的公开 v0.1.0 / v0.1.1 / v0.1.2 | 使用公开 v0.1.3，原正式签名一致，直接覆盖安装 |
| 已安装本次验收的 v0.1.3 build 12 | 与本次发布 APK 相同，无需重复安装 |
| 早期由维护者直接安装的 GIF Converter 内测版 | 联系维护者取得原内测签名的专用升级包，直接覆盖安装；不要用卸载来处理签名报错 |
| 名称为“GIF Converter 兼容测试” | 这是独立包名，公开版可另行安装，两边历史独立 |

## 安装条件

- Android 10 或更高版本，arm64-v8a（64 位 ARM 系统）设备；不包含 32 位 ARM 或 x86 安装包。
- 此前独立兼容测试版已收到 Android 10 与 Android 16 两位用户的正常使用反馈；Android 10 机型未提供，Android 16 为维护者设备 BKQ AN80。本次快捷处理与文字批注在 Android 16 已验收，语言切换、重开及日期已检查；Android 10～15 的这些新功能与真正的 16 KB 页面环境仍需补充测试。
- 请预留素材、临时文件和生成结果需要的存储空间。APK 的文件大小不代表处理任务所需的内存或存储量。

从 [v0.1.3 测试版下载页](https://github.com/LingXia979/GIF-Converter-Releases/releases/tag/v0.1.3)获取 `GIF-Converter-v0.1.3-arm64.apk`，核对同页的 `SHA256SUMS.txt` 后打开安装，按系统提示完成操作。不要下载来源不明的改包，也无需关闭手机的整体安全防护。

## 已安装公开 v0.1.0 / v0.1.1 / v0.1.2 的用户

直接安装新版即可更新，**无需先卸载**。v0.1.3 沿用 `com.local.gifconverter` 包名和原正式发布签名，版本代码升至 12；正常覆盖更新保留应用数据和生成历史。旧任务继续兼容，重要结果仍建议另存。

2026-09-24 已在 BKQ AN80 / Android 16 实际验证：先安装公开 v0.1.0 并生成 1 条图标示例历史，再直接覆盖安装公开 v0.1.1；更新成功，示例记录保留且能打开预览。手机实际安装的 APK 与 GitHub 下载文件 SHA-256 相同。

2026-09-25 同设备从公开 v0.1.1 覆盖到最终 v0.1.2 build 9，原有 8 条历史保留、旧记录可预览，应用 ID、首次安装时间和数据目录身份不变。用户已确认旋转和裁切结果正常；本次发布文件与手机上验收的 APK 哈希一致。

## 以后如何更新

2026-09-26 同设备安装 v0.1.3 build 12 成功，更新前后历史均为 24 条，应用 ID、首次安装时间和数据目录身份不变；最终实际 APK 与发布文件哈希相同。用户已确认快捷处理、文字拖动及指定帧显示正常。

已使用 GitHub 公开正式签名版的用户，后续从[发布页](https://github.com/LingXia979/GIF-Converter-Releases/releases)下载适合设备的新版 APK，打开后按系统提示更新即可，**不需要每次卸载**。公开更新继续沿用同一包名和正式签名，并递增版本代码；这是手动下载安装，不是 App 内自动更新。重要生成结果仍建议另存。

## 已安装“GIF Converter 兼容测试”的用户

该测试版使用独立包名 `com.local.gifconverter.compat10` 和独立签名；正式版可与它同时安装，两边的历史和设置互相独立。测试版中的重要 GIF、PNG 和 ZIP 请先另存，当前没有自动迁移历史的功能。不要为了安装正式版卸载其他仍需保留数据的版本。

## 已安装更早的同包名内部调试版的用户

**正式发布签名与更早内部调试版的签名不同，不能按普通覆盖更新直接替换。** 此限制不适用于从公开 v0.1.0 / v0.1.1 / v0.1.2 更新。

现已准备沿用原内测签名的 v0.1.1 专用升级包，由维护者单独提供。它保持原包名和数据格式，用于不卸载的覆盖更新；这个通道后续仍需使用原内测签名的包。不会因为安装这个升级包就自动迁移成正式签名。

核对实际安装证书后选择对应更新包。如来源不明或仍报错，请提供完整提示、版本和安装来源，由维护者继续核对。Android 10 独立“兼容测试”包使用第三个独立签名及包名，不属于这里的旧同包名内测版。

若希望今后直接使用 GitHub 公开安装包更新，可选择**一次性切换到正式签名版**：先另存需要的 GIF、PNG 和 ZIP，确认接受丢弃 App 内历史和设置，再卸载旧内测版并安装公开 APK。这一步会清除旧 App 内数据；已另存到相册等公共目录的副本保留，但不会自动恢复成历史。当前没有内部历史数据库迁移或一键导入。需要保留旧 App 内数据时，应继续使用对应原签名的更新包，不要卸载。

签名身份由 Android 安装器验证，改变包名显示文字或降低最低系统版本不会消除签名差异。参考 [Android 应用签名说明](https://developer.android.com/studio/publish/app-signing#considerations)。

## 使用与性能提示

从首页选择功能，通过系统选择器选择素材，设置参数后生成。结果可预览、保存到相册或分享；仍保存在 App 内的 GIF 和分解结果可在“生成历史”查看、再次保存或删除。

GIF 结果页点击“编辑 / 压缩 / 拼接”可直接带入当前结果。“GIF 编辑 → 文字批注”支持多段文字、拖动和样式；默认整段显示，关闭该选项可指定原始起止帧。范围包含首尾，删帧或倒放时文字跟随对应原始画面；完成文字设置后再生成。

首页设置 →“语言 / Language”可选跟随系统、中文或 English，立即生效并在重开后保留。系统相册、文件选择器和权限窗口的语言由系统应用决定。

历史页点击“筛选”，可多选功能类型，指定某天或起止日期。日期按手机本地的生成完成时间计算，包含首尾两天；范围日历可点选、输入日期，也可按住首尾圆点拖动，靠近上下边缘可滚动月份。打开记录后返回或刷新会保留筛选条件；筛选内的全选只选择当前显示的结果，改变条件会清除旧勾选。

本地转换会使用处理器、内存和存储，可能增加耗电、使手机发热。建议先用少量图片或短视频，降低尺寸、帧率或抽帧频率。尽量避免边充电边连续处理大任务；明显发烫或系统提示温度过高时，取消任务并让手机降温。当前版本没有自动温度监测或过热自动暂停。

完整数据说明、素材授权要求及责任范围见同一下载目录的 `DISCLAIMER.md`。不同相册对 GIF 循环次数、透明背景和缩放的处理可能不同，请以实际结果为准。

## 配套文件与反馈

本软件使用 FFmpeg 8.1.2（LGPL-2.1-or-later），对应源码、补丁和构建方法在 `FFmpeg-8.1.2-source.zip`。第三方许可正文和组件记录在 `THIRD-PARTY-NOTICES.zip`，也可通过 App 设置中的“开源许可”查看。下载渠道应同时提供这些文件。

遇到问题请向维护者提供版本号、手机型号、Android 版本、功能、参数和复现步骤；示例素材请先确认有权分享，并去除个人信息。无需提供完整私人相册。
