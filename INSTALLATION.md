# GIF Converter v0.1.0 测试版：安装与使用

这是供体验和反馈的 Android 测试版，功能对应原开发进度 v0.54，并补充了“开源许可”入口。安装版本为 **0.1.0**，版本代码为 **2**。

## 安装条件

- Android 12 或更高版本，arm64-v8a（64 位 ARM）设备。
- 目前功能验收主要来自 BKQ AN80 / Android 16 / 4 KB 页面设备。其他系统及真正的 16 KB 页面环境仍需补充测试。
- 请预留素材、临时文件和生成结果需要的存储空间。APK 的文件大小不代表处理任务所需的内存或存储量。

从维护者公布的下载渠道获取 `GIF-Converter-v0.1.0-arm64.apk`，核对 `SHA256SUMS.txt` 后打开安装，按系统提示完成操作。不要下载来源不明的改包，也无需关闭手机的整体安全防护。

## 已安装内部测试版的用户

**本包使用新的正式签名，与此前内部测试包的调试签名不同，不能按普通覆盖更新直接替换。**

先在旧版“生成历史”中把需要的 GIF 保存到相册或分享另存，检查副本能正常打开。PNG / ZIP 等其他重要结果也请另存。卸载旧版会删除应用内的任务记录和未另存文件；相册副本不能自动恢复成新版的生成历史。目前没有实现内部历史数据库迁移或一键导入。

不急于试用时可继续保留旧版。准备改用本包时，在确认重要文件已经另存后，再自行卸载旧版、安装本包。维护者不会为了验证新包而自动卸载用户的旧版应用。

以后同一包名、同一正式签名且版本代码更高的更新，通常可以覆盖安装；更新前仍建议保存重要结果。

## 使用与性能提示

从首页选择功能，通过系统选择器选择素材，设置参数后生成。结果可预览、保存到相册或分享，仍保存在 App 内的 GIF 可在“生成历史”查看和删除。

本地转换会使用处理器、内存和存储，可能增加耗电、使手机发热。建议先用少量图片或短视频，降低尺寸、帧率或抽帧频率。尽量避免边充电边连续处理大任务；明显发烫或系统提示温度过高时，取消任务并让手机降温。当前版本没有自动温度监测或过热自动暂停。

完整数据说明、素材授权要求及责任范围见同一下载目录的 `DISCLAIMER.md`。不同相册对 GIF 循环次数、透明背景和缩放的处理可能不同，请以实际结果为准。

## 配套文件与反馈

本软件使用 FFmpeg 8.1.2（LGPL-2.1-or-later），对应源码、补丁和构建方法在 `FFmpeg-8.1.2-source.zip`。第三方许可正文和组件记录在 `THIRD-PARTY-NOTICES.zip`，也可通过 App 设置中的“开源许可”查看。下载渠道应同时提供这些文件。

遇到问题请向维护者提供版本号、手机型号、Android 版本、功能、参数和复现步骤；示例素材请先确认有权分享，并去除个人信息。无需提供完整私人相册。
